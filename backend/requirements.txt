# FastAPI
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
# EmailStr 은 이 패키지가 있어야 동작한다. pydantic 이 자동으로 끌어오지 않는다.
email-validator>=2.1.1

# Database
sqlalchemy==2.0.36
pymysql==1.1.1
cryptography==44.0.0
alembic==1.14.0

# Queue
celery==5.4.0
redis==5.2.1

# HTTP & Crawling
httpx==0.28.1
beautifulsoup4==4.12.3
readability-lxml==0.8.1
# lxml 5 부터 lxml.html.clean 이 분리되어 readability-lxml 이 별도 패키지를 요구한다
lxml==5.3.0
lxml_html_clean==0.4.1
feedparser==6.0.11

# LLM
# 스트리밍 응답에서 usage 를 받으려면 stream_options 를 지원하는 버전이 필요하다
openai==1.57.4
anthropic==0.40.0

# Storage
boto3==1.35.81

# Utilities
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib==1.7.4
# passlib 1.7.4 는 bcrypt 4.1+ 에서 버전 탐지 경고를 내므로 4.0.x 로 고정한다
bcrypt==4.0.1
python-multipart==0.0.20
email-validator==2.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
Pillow==11.0.0
moto[s3]==5.2.2
